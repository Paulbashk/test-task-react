/* Секция UI */
import inputs from './ui/inputs.config';
import listen from './ui/listen.config';
import buttons from './ui/buttons.config';
import selections from './ui/selections.config';
import accordeon from './ui/accordeon.config';

const ui = {
  title: 'UI',
  inputs,
  listen,
  buttons,
  selections,
  accordeon
};

export default ui;
